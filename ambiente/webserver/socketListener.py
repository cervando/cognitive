from random import randint

import time

connections = dict()


'''
def initConexions():
	i = 0	
	while True:
		time.sleep(randint(0,4))
		#if connections:
		for key in connections:
		 	con = connections[key]["object"]
		 	con.write_message(u"You said: hello " + str(i))
		 	print u"You said: hello " + str(i)
		 	i = i + 1
'''